#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int main()
{


 printf("%d",isupper('A'));

}

 /*
nt min=100;
 int max=0;

 int number;
 int count;
 printf("enter how many students:\n");
 scanf("%d",&count);
 for(int i=0;i<count;i++){
    printf("enter the grades :\n",i+1);
    scanf("%d",&number);
    if(number>max){
            max=number;}


    if(number<min){
        min=number;
    }

    }

    printf("the range grade is %d\n",max-min);

 int sum=0;
 int number;
 int count;
 printf("enter the counting number:\n");
 scanf("%d",&count);
 for(int i=0;i<count;i++){
    printf("enter the item's %d price:\n",i+1);
    scanf("%d",&number);
    sum=sum+number;
 }
 printf(" the total price is %d",sum);
 printf(" the av will be %d",sum/count);
  int count=10;
  while(count>0){

  printf("Hello world! %d\n",count);


  count--;

  //count=count+2;
  //count+=2;
  //count++;
  //count=count+1;
}
*/


